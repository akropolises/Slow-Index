{
  "main": "electron-main.js",
  "scripts": {
    "start": "node dev-server.js",
    "start:push": "node push-server.js",
    "electron": "electron .",
    "check": "node --check app.js && node --check google-calendar.js && node --check micro-slows.js && node --check dev-server.js && node --check push-server.js && node --check sw.js && node --check electron-main.js && node --check electron-preload.js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
