{
  "main": "electron-main.js",
  "scripts": {
    "electron": "electron .",
    "check": "node --check app.js && node --check micro-slows.js && node --check electron-main.js && node --check electron-preload.js"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
